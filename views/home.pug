extends layouts/main
include mixins/videoBlock


block content
    //p=pageTitle // 변수의 일반 text 사용시 pug의 방식
    .videos
        each item in videos
            //h1= item.title
            +videoBlock({
                title: item.title,
                description: item.description
            })